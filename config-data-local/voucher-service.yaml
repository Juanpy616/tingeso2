server:
  port: 0
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/voucher
    username: root
    password: Juanpablo616
    mail:
      host: smtp.mailtrap.io
      port: 587
      username: juanpyloyola@gmail.com
      password: offl eyrz tzzv bslc 
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
            ssl:
              enable: false
          from:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        use_sql_comments: true
        jdbc_time_zone: UTC
        default_schema: voucher
eureka:
  instance:
    preferIpAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/