server:
  port: 0
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL_M1}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: Juanpablo616
eureka:
  instance:
    preferIpAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://discovery-server:8761/eureka/