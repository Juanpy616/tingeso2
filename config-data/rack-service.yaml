server:
  port: 0
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL_RACK}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASURCE_PASSWORD}
eureka:
  instance:
    preferIpAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://discovery-server:8761/eureka/